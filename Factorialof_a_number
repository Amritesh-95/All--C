#include <stdio.h>
void main()
{
    int i, fact = 1, n;

    printf(" Input a number n ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++)
        fact = fact * i;

    printf(" Factorial of given number is = %d", fact);
}
