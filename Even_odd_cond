#include <stdio.h>
void main()
{
    int n;

    printf(" Input a number ");
    scanf("%d", &n);

    if (n % 2 == 0)
    {
        printf(" Number is even = %d", n * n);
    }
    else
    {
        printf(" Number is odd = %d", n * n * n);
    }
}
