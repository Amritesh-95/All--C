#include <stdio.h>
#include <math.h>
void main()
{
    float x, x1, x2, y, y1, y2, D;

    printf(" Please input coordinates ");
    scanf("%f%f%f%f", &x1, &y1, &x2, &y2);

    x = x2 - x1;
    y = y2 - y1;
    D =sqrt((x*x)+(y*y));

    printf(" Distance between the coordinates = %f",D);
}
