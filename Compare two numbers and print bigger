#include <stdio.h>
void main()
{
    int a, b;

    printf(" Please input two number ");
    scanf("%d%d", &a, &b);

    if (a > b)
    {
        printf(" Number is greater = %d", a);
    }
    else
    {
        printf(" Number is greater = %d", b);
    }
}
