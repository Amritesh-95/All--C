#include <stdio.h>
void main()
{
    int a, b;

    printf(" Input two numbers ");
    scanf("%d%d", &a, &b);

    if (a == b)
    {
        printf(" Numbers are equal = %d", a + b);
    }
    else
    {
        printf(" Numbers are not equal = %d", a * b);
    }
}
